services:
  indexer:
    image: akshola00/paymesh-indexer:latest
    restart: always
    environment:
      DNA_TOKEN: "${DNA_TOKEN}"
      API_URL: "${API_URL}"
      PAYMESH_API_KEY: "${PAYMESH_API_KEY}"
